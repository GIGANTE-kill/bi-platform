services:
  db:
    image: postgres:16
    container_name: bi-postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-bi_platform}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - Servidores

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bi-app
    user: "root" # <--- LINHA ADICIONADA AQUI!
    restart: always
    ports:
      - "8089:3000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-bi_platform}?schema=public
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_CONNECT_STRING: ${DB_CONNECT_STRING}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://10.122.83.135:8089}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-pSADIjasd9asd87as98d7asd8a7sd8as7d8asd}
    depends_on:
      - db
    networks:
      - Servidores

  cron:
    build:
      context: .
      dockerfile: Dockerfile.cron
    container_name: bi-cron
    restart: always
    depends_on:
      - app
    networks:
      - Servidores

volumes:
  postgres_data:


networks:
  Servidores:
    external: true
