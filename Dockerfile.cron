FROM alpine:latest

# Install dependencies if needed (wget is usually already in alpine)
RUN apk add --no-cache wget

# Copy the crontab file to the expected location
COPY crontab /etc/crontabs/root

# Run crond in the foreground
CMD ["crond", "-f", "-d", "8"]
